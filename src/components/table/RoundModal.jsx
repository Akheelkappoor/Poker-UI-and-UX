const RoundModal = ({ label }) => (
  <div className="round-modal">
    <div className="round-card">
      <p className="label">Next Round</p>
      <h2>{label}</h2>
    </div>
  </div>
);

export default RoundModal;
